# Invoice Expense System

Initial repository for the Invoice Expense System project.

## Notes
- This repo was initialized locally.
- Add a remote with `git remote add origin <url>` if you want to push.
